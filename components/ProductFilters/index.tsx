import { View, Text } from "react-native";

const ProductFilters: React.FC = () => {
    return (
        <View>
            <Text>Filters</Text>
        </View>
    );
};

export default ProductFilters;